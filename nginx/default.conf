server {
    listen 80;
    root /opt/app/dist;

    location / {
        try_files $uri.html $uri/ /404.html;
    }

    location /api/ {
        proxy_pass https://crowdsourcing-backend-306wins.app.secoder.net/;
    }
}
